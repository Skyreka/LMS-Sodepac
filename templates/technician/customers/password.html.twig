{% extends 'base.html.twig' %}

{% block title %}Editer {{ user.firstname }} {{ user.lastname }}{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row page-titles">
            <div class="col-md-5 col-8 align-self-center">
                <h3 class="text-themecolor">Modification du mot de passe de {{ user.firstname }} {{ user.lastname }}</h3>
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="{{ path('login.success') }}">Accueil</a></li>
                    <li class="breadcrumb-item"><a href="{{ path('technician_customers_index') }}">Clients</a></li>
                    <li class="breadcrumb-item active">Modification de mot de passe</li>
                </ol>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        {% include 'technician/customers/_password.html.twig' %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}