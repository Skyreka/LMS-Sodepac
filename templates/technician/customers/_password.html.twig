{{ form_start(form) }}
    <div class="row">
        <div class="col-6">
            {{ form_row(form.password) }}
        </div>
    </div>
    {{ form_rest(form) }}
    <button class="btn btn-primary">Modifier le mot de passe</button>
{{ form_end(form) }}