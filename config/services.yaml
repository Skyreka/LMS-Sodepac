# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    admin_prefix:     '%env(resolve:ADMIN_PREFIX)%'
    locale: 'fr'
    flash_directory: '%kernel.project_dir%/public/uploads/flash'
    chat_message_directory: '%kernel.project_dir%/public/uploads/messages'
    panorama_directory: '%kernel.project_dir%/public/uploads/panorama'
    recommendation_directory: '%kernel.project_dir%/public/uploads/recommendations'
    APP_NAME: '%env(APP_NAME)%'
    admin_email: '%env(resolve:ADMIN_EMAIL)%'
    dkim_key: '%env(resolve:MAILER_DKIM)%'
    dkim_selector: '%env(MAILER_DKIM_SELECTOR)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        public: false       # Allows optimizing the container by removing unused services; this also means
                            # fetching services directly from the container via $container->get() won't work.
                            # The best practice is to be explicit about your dependencies anyway.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/*'
        exclude: '../src/{DependencyInjection,Entity,Infrastructure/Migrations,Tests,Kernel.php}'

    # controllers are imported separately to make sure services can be injected
    # as action arguments even if you don't extend any base controller class
    App\Http\Controller\:
        resource: '../src/Http/Controller'
        tags: ['controller.service_arguments']

    App\Http\Admin\Controller\:
        resource: '../src/Http/Admin/Controller'
        tags: [ 'controller.service_arguments' ]

    App\Infrastructure\Mailing\MailerService:
        class: App\Infrastructure\Mailing\MailerService
        arguments:
            $dkimKey: '%dkim_key%',
            $dkimSelector: '%dkim_selector%'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
    # datatable handler
    datatable.users:
        class: App\Http\DataTables\UsersDataTables
        tags: [{ name: datatable, id: users }]
        arguments: [
            '@doctrine',
            '@router'
        ]
    datatable.products:
        class: App\Http\DataTables\ProductsDataTables
        tags: [{ name: datatable, id: products }]
        arguments: [
            '@doctrine',
            '@router'
        ]
    datatable.pricing:
        class: App\Http\DataTables\PricingDataTables
        tags: [ { name: datatable, id: pricing } ]
        arguments: [
                '@doctrine',
                '@router'
        ]

